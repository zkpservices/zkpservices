export const description =
  'On this page, we’ll dive into the different user endpoints you can use to manage user data programmatically.'

# User Data

User data is the foundation of the zkp.services solution. On this page, we will dive into the API requests that our
 dApp makes to the backend API to manage your data securely. Our serverless dApp frontend also allows you to 
 specify your own custom URL for your data provider. In order to interact with the protocol through this particular
 frontend, the data source needs to be compliant with these API guidelines and the custom URL needs to be specified
 when logging in. {{ className: 'lead' }} 

## The structure of user data

Our data model is an extremely versatile JSON object that gives way to endless data points a user can store for
 countless different services. As such, below, we will provide an example of a potential data structure for user
  data, but note that none of the fields in the data object are specifically required.
We will support other data and object types in the future.

### Properties of JSON object

<Properties>
  <Property name="id" type="string">
    Unique wallet address identifier for the user.
  </Property>
  <Property name="password" type="string">
    The password associated with the user.
  </Property>
  <Property name="2fa_password" type="string">
    The 2FA password associated with the user.
  </Property>
  <Property name="contract_password" type="string">
    The smart contract password associated with the user.
  </Property>
  <Property name="chain_data" type="JSON Object[]">
    JSON object containing the entire dataset for a specific user, where each
    chain ID they have onboarded to is a child of this object.
  </Property>
</Properties>

---

## Retrieve user data {{ tag: 'POST', label: '/userdata, action: "get_item"' }}

<Row>
  <Col>

    This endpoint allows you to retrieve either the entire or a subset of the "data" object for the self-user.

    Authorization is required.

    ### Required attributes

    <Properties>
      <Property name="id" type="string">
        The wallet address of the user.
      </Property>
      <Property name="password" type="string">
        The password of the user.
      </Property>
      <Property name="action" type="string">
        The action of this request, which is "get_item".
      </Property>
      <Property name="chain_id" type="string">
        The chain id of the dataset to be pulled.
      </Property>
    </Properties>

    ### Optional attributes

    <Properties>
      <Property name="key" type="string">
        The flattened JSON key for the property trying to be retrieved, for example, "medical records - pharmacy central melbourne.data.active prescriptions".
        If not provided, it will return the entire data set for the user.
      </Property>
    </Properties>

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="POST" label="/userdata">

    ```bash {{ title: 'cURL' }}
    curl --location 'https://api.zkp.services/userdata' \
    --header 'Content-Type: application/json' \
    --data '{
          "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
          "action": "get_item",
          "password": "abc123",
          "chain_id": "0xa869",
          "key": "medical records"

    }'
    ```

    ```js
    var settings = {
      "url": "https://api.zkp.services/userdata",
      "method": "POST",
      "timeout": 0,
      "headers": {
        "Content-Type": "application/json"
      },
      "data": JSON.stringify({
        "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
        "action": "get_item",
        "password": "abc123",
        "chain_id": "0xa869",
        "key": "medical records"
      }),
    };

    $.ajax(settings).done(function (response) {
      console.log(response);
    });

    $.ajax(settings).done(function (response) {
      console.log(response);
    });
    ```

    ```python
    import requests
    import json

    url = "https://api.zkp.services/userdata"

    payload = json.dumps({
      "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
      "action": "get_item",
      "password": "abc123",
      "chain_id": "0xa869",
      "key": "medical records"
    })
    headers = {
      'Content-Type': 'application/json'
    }

    response = requests.request("POST", url, headers=headers, data=payload)

    print(response.text)


    ```

    ```powershell {{ title: 'Powershell' }}
    $headers = New-Object "System.Collections.Generic.Dictionary[[String],[String]]"
    $headers.Add("Content-Type", "application/json")

    $body = @"
    {
          `"id`": `"0xd7b250aa0c88def87cb6b92b6d40b5b52ab3507e`",
          `"action`": `"get_item`",
          `"password`": `"abc123`",
          `"chain_id`": `"0xa869`",
          `"key`": `"medical records`"

    }
    "@

    $response = Invoke-RestMethod 'https://api.zkp.services/userdata' -Method 'POST' -Headers $headers -Body $body
    $response | ConvertTo-Json
    ```

    </CodeGroup>

    ```json {{ title: 'Response' }}
    {
        "medical records": {
            "usage": "Antibiotic",
            "dosage": "250mg to 500mg every 8 hours or 875mg every 12 hours",
            "refills remaining": "3 for 50 x 1g or equivalent"
        }
    }
    ```

  </Col>
</Row>

---

## Create a user and dataset {{ tag: 'POST', label: '/userdata, action: "create_item"' }}

<Row>
  <Col>

    This endpoint allows you to create a new user and associated dataset. You can only add a user from a wallet that you own.

    ### Required attributes

    <Properties>
      <Property name="id" type="string">
        The wallet address of the user.
      </Property>
      <Property name="password" type="string">
        The password of the user.
      </Property>
      <Property name="action" type="string">
        The action of this request, which is "create_item".
      </Property>
      <Property name="chain_data" type="string">
        Contains the initial JSON dataset for a user. Can contain multiple chains.
      </Property>
      <Property name="2fa_password" type="string">
        The 2FA password of the user.
      </Property>
      <Property name="contract_password" type="string">
        The smart contract password of the user.
      </Property>


    </Properties>

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="POST" label="/userdata">

    ```bash {{ title: 'cURL' }}
    curl --location 'https://api.zkp.services/userdata' \
    --header 'Content-Type: application/json' \
    --data '{
        "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
        "chain_id": "0xa869",
        "password": "abc123",
        "action": "create_item",
        "contract_password": "abc123",
        "2fa_password": "abc123",
        "chain_data": {
            "0xa869": {
                "rsa_enc_pub_key": "<public key>",
                "rsa_enc_priv_key": "<private key>",
                "rsa_sign_pub_key": "<public key>",
                "rsa_sign_priv_key": "<private key>",
                "public_info": "Hello"
            }
        }
    }'
    ```

    ```js
    var settings = {
      "url": "https://api.zkp.services/userdata",
      "method": "POST",
      "timeout": 0,
      "headers": {
        "Content-Type": "application/json"
      },
      "data": JSON.stringify({
        "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
        "chain_id": "0xa869",
        "password": "abc123",
        "action": "create_item",
        "contract_password": "abc123",
        "2fa_password": "abc123",
        "chain_data": {
          "0xa869": {
            "rsa_enc_pub_key": "<public key>",
            "rsa_enc_priv_key": "<private key>",
            "rsa_sign_pub_key": "<public key>",
            "rsa_sign_priv_key": "<private key>",
            "public_info": "Hello"
          }
        }
      }),
    };

    $.ajax(settings).done(function (response) {
      console.log(response);
    });

$.ajax(settings).done(function (response) {
console.log(response);
});
```

    ```python
    import requests
    import json

    url = "https://api.zkp.services/userdata"

    payload = json.dumps({
      "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
      "chain_id": "0xa869",
      "password": "abc123",
      "action": "create_item",
      "contract_password": "abc123",
      "2fa_password": "abc123",
      "chain_data": {
        "0xa869": {
          "rsa_enc_pub_key": "<public key>",
          "rsa_enc_priv_key": "<private key>",
          "rsa_sign_pub_key": "<public key>",
          "rsa_sign_priv_key": "<private key>",
          "public_info": "Hello"
        }
      }
    })
    headers = {
      'Content-Type': 'application/json'
    }

    response = requests.request("POST", url, headers=headers, data=payload)

    print(response.text)
    ```

    ```powershell {{ title: 'Powershell' }}
    $headers = New-Object "System.Collections.Generic.Dictionary[[String],[String]]"
    $headers.Add("Content-Type", "application/json")

    $body = @"
    {
        `"id`": `"0xce231517da5940e0880c6a2062255640916016ba`",
        `"chain_id`": `"0xa869`",
        `"password`": `"abc123`",
        `"action`": `"create_item`",
        `"contract_password`": `"abc123`",
        `"2fa_password`": `"abc123`",
        `"chain_data`": {
            `"0xa869`": {
                `"rsa_enc_pub_key`": `"<public key>`",
                `"rsa_enc_priv_key`": `"<private key>`",
                `"rsa_sign_pub_key`": `"<public key>`",
                `"rsa_sign_priv_key`": `"<private key>`",
                `"public_info`": `"Hello`"
            }
        }
    }
    "@

    $response = Invoke-RestMethod 'https://api.zkp.services/userdata' -Method 'POST' -Headers $headers -Body $body
    $response | ConvertTo-Json
    ```

    </CodeGroup>

    ```text {{ title: 'Response' }}
    Item created successfully!
    ```

  </Col>
</Row>

---

## Update a users data {{ tag: 'PUT', label: '/userdata, action: "update_item"' }}

<Row>
  <Col>

    This endpoint allows you to update the data object for a specific user. <br/><br/>
    This is an <b>incremental</b> function, meaning that you supply the data \{\} json object containing only the fields you wish to update. The fields you do not specify will not be modified. If you specify a field that does not exist, it will be created.

    ### Required attributes

    <Properties>
      <Property name="id" type="string">
        The wallet address of the user.
      </Property>
      <Property name="password" type="string">
        The password of the user.
      </Property>
      <Property name="action" type="string">
        The action of this request, which is "update_item".
      </Property>
      <Property name="data" type="JSON Object">
        Contains the updated data for the user.
      </Property>
      <Property name="chain_id" type="string">
        The current chain ID.
      </Property>
    </Properties>

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="PUT" label="/userdata">

    ```bash {{ title: 'cURL' }}
    curl --location --request PUT 'https://api.zkp.services/userdata' \
    --header 'Content-Type: application/json' \
    --data '{
          "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
          "action": "update_item",
          "password": "abc123",
          "chain_id": "0xa869",
          "data": {
              "myki": {
                  "balance": "A$11.00"
              }
          }
    }'
    ```

    ```js
    var settings = {
      "url": "https://api.zkp.services/userdata",
      "method": "PUT",
      "timeout": 0,
      "headers": {
        "Content-Type": "application/json"
      },
      "data": JSON.stringify({
        "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
        "action": "update_item",
        "password": "abc123",
        "chain_id": "0xa869",
        "data": {
          "myki": {
            "balance": "A$11.00"
          }
        }
      }),
    };

    $.ajax(settings).done(function (response) {
      console.log(response);
    });
    ```

    ```python
    import requests
    import json

    url = "https://api.zkp.services/userdata"

    payload = json.dumps({
      "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
      "action": "update_item",
      "password": "abc123",
      "chain_id": "0xa869",
      "data": {
        "myki": {
          "balance": "A$11.00"
        }
      }
    })
    headers = {
      'Content-Type': 'application/json'
    }

    response = requests.request("PUT", url, headers=headers, data=payload)

    print(response.text)

    ```

    ```powershell {{ title: 'Powershell' }}
    $headers = New-Object "System.Collections.Generic.Dictionary[[String],[String]]"
    $headers.Add("Content-Type", "application/json")

    $body = @"
    {
          `"id`": `"0xd7b250aa0c88def87cb6b92b6d40b5b52ab3507e`",
          `"action`": `"update_item`",
          `"password`": `"abc123`",
          `"chain_id`": `"0xa869`",
          `"data`": {
              `"myki`": {
                  `"balance`": `"A`$11.00`"
              }
          }
    }
    "@

    $response = Invoke-RestMethod 'https://api.zkp.services/userdata' -Method 'PUT' -Headers $headers -Body $body
    $response | ConvertTo-Json
    ```

  </CodeGroup>
      ```json {{ title: 'Response' }}
      {
          "data": {
              "myki": {
                  "balance": "A$200"
              },
              "id": {
                  "name": "Johnathan Doe",
                  "DOB": "25/03/2002"
              },
              "NAB": {
                  "savings": "A$2400.00"
              }
          }
      }
    ```
  </Col>
</Row>

---

## Update password {{ tag: 'PUT', label: '/userdata, action: "update_item"' }}

<Row>
  <Col>

    This endpoint allows you to update your password.

    Authentication is required

    ### Required attributes

    <Properties>
      <Property name="id" type="string">
        The wallet address of the user.
      </Property>
      <Property name="password" type="string">
        The current password of the user.
      </Property>
      <Property name="action" type="string">
        The action of this request, which is "update_password".
      </Property>
      <Property name="new_password" type="string">
        The new password.
      </Property>
    </Properties>

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="PUT" label="/userdata">

    ```bash {{ title: 'cURL' }}
    curl --location --request PUT 'https://api.zkp.services/userdata' \
    --header 'Content-Type: application/json' \
    --data-raw '{
            "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
            "new_password": "abc123",
            "password": "abc123",
            "action": "update_password"
    }'
    ```

    ```js
    var settings = {
      "url": "https://api.zkp.services/userdata",
      "method": "PUT",
      "timeout": 0,
      "headers": {
        "Content-Type": "application/json"
      },
      "data": JSON.stringify({
        "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
        "new_password": "abc123",
        "password": "abc123",
        "action": "update_password"
      }),
    };

    $.ajax(settings).done(function (response) {
      console.log(response);
    });
    ```

    ```python
    import requests
    import json

    url = "https://api.zkp.services/userdata"

    payload = json.dumps({
      "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
      "new_password": "abc123",
      "password": "abc123",
      "action": "update_password"
    })
    headers = {
      'Content-Type': 'application/json'
    }

    response = requests.request("PUT", url, headers=headers, data=payload)

    print(response.text)
    ```

    ```powershell
    $headers = New-Object "System.Collections.Generic.Dictionary[[String],[String]]"
    $headers.Add("Content-Type", "application/json")

    $body = "{
    `n        `"id`": `"0xd7b250aa0c88def87cb6b92b6d40b5b52ab3507e`",
    `n        `"new_password`": `"abc123`",
    `n        `"password`": `"abc123`",
    `n        `"action`": `"update_password`"
    `n}"

    $response = Invoke-RestMethod 'https://api.zkp.services/userdata' -Method 'PUT' -Headers $headers -Body $body
    $response | ConvertTo-Json
    ```

    </CodeGroup>

    ```text {{ title: 'Response' }}
    "Password updated successfully!"
    ```

  </Col>
</Row>

---

## Login {{ tag: 'POST', label: '/userdata, action: "login"' }}

<Row>
  <Col>

    This endpoint allows you to authenticate with our dApp.

    ### Required attributes

    <Properties>
      <Property name="id" type="string">
        The wallet address of the user.
      </Property>
      <Property name="password" type="string">
        The password of the user.
      </Property>
      <Property name="action" type="string">
        The action of this request, which is "login".
      </Property>
      <Property name="chain_id" type="string">
        The current chain ID.
      </Property>
    </Properties>

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="POST" label="/userdata">

    ```bash {{ title: 'cURL' }}
    curl --location 'https://api.zkp.services/userdata' \
    --header 'Content-Type: application/json' \
    --data '{
          "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
          "action": "login",
          "password": "pop",
          "chain_id": "0xa869"
    }'
    ```

    ```js
    var settings = {
      "url": "https://api.zkp.services/userdata",
      "method": "POST",
      "timeout": 0,
      "headers": {
        "Content-Type": "application/json"
      },
      "data": JSON.stringify({
        "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
        "action": "login",
        "password": "pop",
        "chain_id": "0xa869"
      }),
    };

    $.ajax(settings).done(function (response) {
      console.log(response);
    });
    ```

    ```python
    import requests
    import json

    url = "https://api.zkp.services/userdata"

    payload = json.dumps({
      "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
      "action": "login",
      "password": "pop",
      "chain_id": "0xa869"
    })
    headers = {
      'Content-Type': 'application/json'
    }

    response = requests.request("POST", url, headers=headers, data=payload)

    print(response.text)

    ```

    ```powershell
    $headers = New-Object "System.Collections.Generic.Dictionary[[String],[String]]"
    $headers.Add("Content-Type", "application/json")

    $body = @"
    {
          `"id`": `"0xd7b250aa0c88def87cb6b92b6d40b5b52ab3507e`",
          `"action`": `"login`",
          `"password`": `"pop`",
          `"chain_id`": `"0xa869`"
    }
    "@

    $response = Invoke-RestMethod 'https://api.zkp.services/userdata' -Method 'POST' -Headers $headers -Body $body
    $response | ConvertTo-Json
    ```

    </CodeGroup>

    ```text {{ title: 'Response' }}
    "User authenticated successfully."
    ```

  </Col>
</Row>

---

## Add request {{ tag: 'POST', label: '/userdata, action: "add_request"' }}

<Row>
  <Col>

    This endpoint allows you to add a new request for data for a specific user.

    Authentication is required.

    ### Required attributes

    <Properties>
      <Property name="id" type="string">
        The wallet address of the user.
      </Property>
      <Property name="password" type="string">
        The password of the user.
      </Property>
      <Property name="action" type="string">
        The action of this request, which is "add_request".
      </Property>
      <Property name="request" type="JSON Object">
        A JSON object containing the request.
      </Property>
    </Properties>

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="POST" label="/userdata">

    ```bash {{ title: 'cURL' }}
    curl --location 'https://api.zkp.services/userdata' \
    --header 'Content-Type: application/json' \
    --data '{
        "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
        "action": "add_request",
        "password": "abc123",
        "request": {
            "requestID": "20716965315651526627121156873417490254946957749778301596059776852466841093871",
            "address_sender": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
            "address_receiver": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
            "attach_token": false,
            "key": "49|imnkx;!9k{9YLMaPR(]eY",
            "chainID": "0xa869",
            "operation": "update",
            "field": "medical data",
            "updated_data": {
                "blood type": "AB"
            },
            "limit": "86400",
            "require2FA": false,
            "response_fee": "10",
            "timestamp": "1701715461439",
            "twoFARequestID": ""
        }
    }'
    ```

    ```js

var settings = {
"url": "https://api.zkp.services/userdata",
"method": "POST",
"timeout": 0,
"headers": {
"Content-Type": "application/json"
},
"data": JSON.stringify({
"id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
"action": "add_request",
"password": "abc123",
"request": {
"requestID": "20716965315651526627121156873417490254946957749778301596059776852466841093871",
"address_sender": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
"address_receiver": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
"attach_token": false,
"key": "49|imnkx;!9k{9YLMaPR(]eY",
"chainID": "0xa869",
"operation": "update",
"field": "medical data",
"updated_data": {
"blood type": "AB"
},
"limit": "86400",
"require2FA": false,
"response_fee": "10",
"timestamp": "1701715461439",
"twoFARequestID": ""
}
}),
};

$.ajax(settings).done(function (response) {
console.log(response);
});
```

    ```python
    import requests
    import json

    url = "https://api.zkp.services/userdata"

    payload = json.dumps({
      "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
      "action": "add_request",
      "password": "abc123",
      "request": {
        "requestID": "20716965315651526627121156873417490254946957749778301596059776852466841093871",
        "address_sender": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
        "address_receiver": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
        "attach_token": False,
        "key": "49|imnkx;!9k{9YLMaPR(]eY",
        "chainID": "0xa869",
        "operation": "update",
        "field": "medical data",
        "updated_data": {
          "blood type": "AB"
        },
        "limit": "86400",
        "require2FA": False,
        "response_fee": "10",
        "timestamp": "1701715461439",
        "twoFARequestID": ""
      }
    })
    headers = {
      'Content-Type': 'application/json'
    }

    response = requests.request("POST", url, headers=headers, data=payload)

    print(response.text)

    ```

    ```powershell
    $headers = New-Object "System.Collections.Generic.Dictionary[[String],[String]]"
    $headers.Add("Content-Type", "application/json")

    $body = @"
    {
        `"id`": `"0xc6165a271f5cb5960c4554a8b44fcf3c75fa7f6c`",
        `"action`": `"add_request`",
        `"password`": `"abc123`",
        `"request`": {
            `"requestID`": `"20716965315651526627121156873417490254946957749778301596059776852466841093871`",
            `"address_sender`": `"0xc6165a271f5cb5960c4554a8b44fcf3c75fa7f6c`",
            `"address_receiver`": `"0xd7b250aa0c88def87cb6b92b6d40b5b52ab3507e`",
            `"attach_token`": false,
            `"key`": `"49|imnkx;!9k{9YLMaPR(]eY`",
            `"chainID`": `"0xa869`",
            `"operation`": `"update`",
            `"field`": `"medical data`",
            `"updated_data`": {
                `"blood type`": `"AB`"
            },
            `"limit`": `"86400`",
            `"require2FA`": false,
            `"response_fee`": `"10`",
            `"timestamp`": `"1701715461439`",
            `"twoFARequestID`": `"`"
        }
    }
    "@

    $response = Invoke-RestMethod 'https://api.zkp.services/userdata' -Method 'POST' -Headers $headers -Body $body
    $response | ConvertTo-Json
    ```

    </CodeGroup>

    ```text {{ title: 'Response' }}
    "Request added successfully"
    ```

  </Col>
</Row>

---

## Add response {{ tag: 'POST', label: '/userdata, action: "add_response"' }}

<Row>
  <Col>

    This endpoint allows you to add a new response for data/update for a specific user.

    Authentication is required.

    ### Required attributes

    <Properties>
      <Property name="id" type="string">
        The wallet address of the user.
      </Property>
      <Property name="password" type="string">
        The password of the user.
      </Property>
      <Property name="action" type="string">
        The action of this request, which is "add_response".
      </Property>
      <Property name="response" type="JSON Object">
        A JSON object containing the response.
      </Property>
    </Properties>

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="POST" label="/userdata">

    ```bash {{ title: 'cURL' }}
    curl --location 'https://api.zkp.services/userdata' \
    --header 'Content-Type: application/json' \
    --data '{
        "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
        "action": "add_response",
        "password": "abc123",
        "response": {
            "responseID": "20716965315651526627121156873417490254946957749778301596059776852466841093871",
            "address_sender": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
            "address_receiver": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
            "attach_token": false,
            "key": "49|imnkx;!9k{9YLMaPR(]eY",
            "chainID": "0xa869",
            "operation": "update",
            "field": "medical data",
            "updated_data": {
                "blood type": "AB"
            },
            "limit": "86400",
            "require2FA": false,
            "timestamp": "1701715461439",
            "twoFARequestID": ""
        }
    }'
    ```

    ```js
    var settings = {
      "url": "https://api.zkp.services/userdata",
      "method": "POST",
      "timeout": 0,
      "headers": {
        "Content-Type": "application/json"
      },
      "data": JSON.stringify({
        "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
        "action": "add_response",
        "password": "abc123",
        "response": {
          "responseID": "20716965315651526627121156873417490254946957749778301596059776852466841093871",
          "address_sender": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
          "address_receiver": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
          "attach_token": false,
          "key": "49|imnkx;!9k{9YLMaPR(]eY",
          "chainID": "0xa869",
          "operation": "update",
          "field": "medical data",
          "updated_data": {
            "blood type": "AB"
          },
          "limit": "86400",
          "require2FA": false,
          "timestamp": "1701715461439",
          "twoFARequestID": ""
        }
      }),
    };

    $.ajax(settings).done(function (response) {
      console.log(response);
    });
    ```

    ```python
    import requests
    import json

    url = "https://api.zkp.services/userdata"

    payload = json.dumps({
      "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
      "action": "add_response",
      "password": "abc123",
      "response": {
        "responseID": "20716965315651526627121156873417490254946957749778301596059776852466841093871",
        "address_sender": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
        "address_receiver": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
        "attach_token": False,
        "key": "49|imnkx;!9k{9YLMaPR(]eY",
        "chainID": "0xa869",
        "operation": "update",
        "field": "medical data",
        "updated_data": {
          "blood type": "AB"
        },
        "limit": "86400",
        "require2FA": False,
        "timestamp": "1701715461439",
        "twoFARequestID": ""
      }
    })
    headers = {
      'Content-Type': 'application/json'
    }

    response = requests.request("POST", url, headers=headers, data=payload)

    print(response.text)

    ```

    ```powershell
    $headers = New-Object "System.Collections.Generic.Dictionary[[String],[String]]"
    $headers.Add("Content-Type", "application/json")

    $body = @"
    {
        `"id`": `"0xc6165a271f5cb5960c4554a8b44fcf3c75fa7f6c`",
        `"action`": `"add_response`",
        `"password`": `"abc123`",
        `"response`": {
            `"responseID`": `"20716965315651526627121156873417490254946957749778301596059776852466841093871`",
            `"address_sender`": `"0xc6165a271f5cb5960c4554a8b44fcf3c75fa7f6c`",
            `"address_receiver`": `"0xd7b250aa0c88def87cb6b92b6d40b5b52ab3507e`",
            `"attach_token`": false,
            `"key`": `"49|imnkx;!9k{9YLMaPR(]eY`",
            `"chainID`": `"0xa869`",
            `"operation`": `"update`",
            `"field`": `"medical data`",
            `"updated_data`": {
                `"blood type`": `"AB`"
            },
            `"limit`": `"86400`",
            `"require2FA`": false,
            `"timestamp`": `"1701715461439`",
            `"twoFARequestID`": `"`"
        }
    }
    "@

    $response = Invoke-RestMethod 'https://api.zkp.services/userdata' -Method 'POST' -Headers $headers -Body $body
    $response | ConvertTo-Json
    ```

    </CodeGroup>

    ```text {{ title: 'Response' }}
    "Response added successfully"
    ```

  </Col>
</Row>

---

## Retrieve user available dashboard items {{ tag: 'POST', label: '/userdata, action: "get_available_dashboard"' }}

<Row>
  <Col>

    This endpoint allows you to the list of services available for a particular user's dashboard.

    Authorization is required.

    ### Required attributes

    <Properties>
      <Property name="id" type="string">
        The wallet address of the user.
      </Property>
      <Property name="password" type="string">
        The password of the user.
      </Property>
      <Property name="action" type="string">
        The action of this request, which is "get_available_dashboard".
      </Property>
      <Property name="chain_id" type="string">
        The current chain ID.
      </Property>
    </Properties>

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="POST" label="/userdata">

    ```bash {{ title: 'cURL' }}
    curl --location 'https://api.zkp.services/userdata' \
    --header 'Content-Type: application/json' \
    --data '{
          "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
          "action": "get_dashboard",
          "password": "abc123",
          "chain_id": "0xa869"
    }'
    ```

    ```js
    var settings = {
      "url": "https://api.zkp.services/userdata",
      "method": "POST",
      "timeout": 0,
      "headers": {
        "Content-Type": "application/json"
      },
      "data": JSON.stringify({
        "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
        "action": "get_dashboard",
        "password": "abc123",
        "chain_id": "0xa869"
      }),
    };

    $.ajax(settings).done(function (response) {
      console.log(response);
    });
    ```

    ```python
    import requests
    import json

    url = "https://api.zkp.services/userdata"

    payload = json.dumps({
      "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
      "action": "get_dashboard",
      "password": "abc123",
      "chain_id": "0xa869"
    })
    headers = {
      'Content-Type': 'application/json'
    }

    response = requests.request("POST", url, headers=headers, data=payload)

    print(response.text)
    ```

    ```powershell {{ title: 'Powershell' }}
    $headers = New-Object "System.Collections.Generic.Dictionary[[String],[String]]"
    $headers.Add("Content-Type", "application/json")

    $body = @"
    {
          `"id`": `"0xce231517da5940e0880c6a2062255640916016ba`",
          `"action`": `"get_dashboard`",
          `"password`": `"abc123`",
          `"chain_id`": `"0xa869`"
    }
    "@

    $response = Invoke-RestMethod 'https://api.zkp.services/userdata' -Method 'POST' -Headers $headers -Body $body
    $response | ConvertTo-Json
    ```

    </CodeGroup>

    ```json {{ title: 'Response' }}
    ["myki", "commbank", "NAB"]
    ```

  </Col>
</Row>

---

## Retrieve user current dashboard items {{ tag: 'POST', label: '/userdata, action: "get_dashboard"' }}

<Row>
  <Col>

    This endpoint allows you to access the list of currently selected services for a particular user's dashboard.

    Authorization is required.

    ### Required attributes

    <Properties>
      <Property name="id" type="string">
        The wallet address of the user.
      </Property>
      <Property name="password" type="string">
        The password of the user.
      </Property>
      <Property name="action" type="string">
        The action of this request, which is "get_dashboard".
      </Property>
      <Property name="chain_id" type="string">
        The current chain ID.
      </Property>
    </Properties>

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="POST" label="/userdata">

    ```bash {{ title: 'cURL' }}
    curl --location 'https://api.zkp.services/userdata' \
    --header 'Content-Type: application/json' \
    --data '{
          "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
          "action": "get_available_dashboard",
          "password": "abc123",
          "chain_id": "0xa869"
    }'
    ```

    ```js
    var settings = {
      "url": "https://api.zkp.services/userdata",
      "method": "POST",
      "timeout": 0,
      "headers": {
        "Content-Type": "application/json"
      },
      "data": JSON.stringify({
        "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
        "action": "get_available_dashboard",
        "password": "abc123",
        "chain_id": "0xa869"
      }),
    };

    $.ajax(settings).done(function (response) {
      console.log(response);
    });
    ```

    ```python
    import requests
    import json

    url = "https://api.zkp.services/userdata"

    payload = json.dumps({
      "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
      "action": "get_available_dashboard",
      "password": "abc123",
      "chain_id": "0xa869"
    })
    headers = {
      'Content-Type': 'application/json'
    }

    response = requests.request("POST", url, headers=headers, data=payload)

    print(response.text)
    ```

    ```powershell {{ title: 'Powershell' }}
    $headers = New-Object "System.Collections.Generic.Dictionary[[String],[String]]"
    $headers.Add("Content-Type", "application/json")

    $body = @"
    {
          `"id`": `"0xc6165a271f5cb5960c4554a8b44fcf3c75fa7f6c`",
          `"action`": `"get_available_dashboard`",
          `"password`": `"abc123`",
          `"chain_id`": `"0xa869`"
    }
    "@

    $response = Invoke-RestMethod 'https://api.zkp.services/userdata' -Method 'POST' -Headers $headers -Body $body
    $response | ConvertTo-Json
    ```

    </CodeGroup>

    ```json {{ title: 'Response' }}
    ["myki", "commbank"]
    ```

  </Col>
</Row>

---

## Add a service to a users dashboard {{ tag: 'POST', label: '/userdata, action: "add_to_dashbaord"' }}

<Row>
  <Col>

    This endpoint allows you to add a particular service to a user's dashboard.

    Authorization is required.

    ### Required attributes

    <Properties>
      <Property name="id" type="string">
        The wallet address of the user.
      </Property>
      <Property name="password" type="string">
        The password of the user.
      </Property>
      <Property name="action" type="string">
        The action of this request, which is "add_to_dashboard".
      </Property>
      <Property name="service" type="string">
        The name of the service you want to add to the user's dashboard.
      </Property>
      <Property name="chain_id" type="string">
        The current chain ID.
      </Property>
    </Properties>

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="POST" label="/userdata">

    ```bash {{ title: 'cURL' }}
    curl --location 'https://api.zkp.services/userdata' \
    --header 'Content-Type: application/json' \
    --data '{
          "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
          "action": "add_to_dashboard",
          "password": "abc123",
          "service": "id",
          "chain_id": "0xa869"
    }'
    ```

    ```js
    var settings = {
      "url": "https://api.zkp.services/userdata",
      "method": "POST",
      "timeout": 0,
      "headers": {
        "Content-Type": "application/json"
      },
      "data": JSON.stringify({
        "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
        "action": "add_to_dashboard",
        "password": "abc123",
        "service": "id",
        "chain_id": "0xa869"
      }),
    };

    $.ajax(settings).done(function (response) {
      console.log(response);
    });
    ```

    ```python
    import requests
    import json

    url = "https://api.zkp.services/userdata"

    payload = json.dumps({
      "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
      "action": "add_to_dashboard",
      "password": "abc123",
      "service": "id",
      "chain_id": "0xa869"
    })
    headers = {
      'Content-Type': 'application/json'
    }

    response = requests.request("POST", url, headers=headers, data=payload)

    print(response.text)
    ```

    ```powershell {{ title: 'Powershell' }}
    $headers = New-Object "System.Collections.Generic.Dictionary[[String],[String]]"
    $headers.Add("Content-Type", "application/json")

    $body = @"
    {
          `"id`": `"0xc6165a271f5cb5960c4554a8b44fcf3c75fa7f6c`",
          `"action`": `"add_to_dashboard`",
          `"password`": `"abc123`",
          `"service`": `"id`",
          `"chain_id`": `"0xa869`"
    }
    "@

    $response = Invoke-RestMethod 'https://api.zkp.services/userdata' -Method 'POST' -Headers $headers -Body $body
    $response | ConvertTo-Json
    ```

    </CodeGroup>

    ```text {{ title: 'Response' }}
    "Item added to dashboard successfully!"
    ```

  </Col>
</Row>
---

## Remove a service from a users dashboard {{ tag: 'DELETE', label: '/userdata, action: "remove_from_dashboard"' }}

<Row>
  <Col>

    This endpoint allows you to remove a particular service from a user's dashboard.

    Authorization is required.

    ### Required attributes

    <Properties>
      <Property name="id" type="string">
        The wallet address of the user.
      </Property>
      <Property name="password" type="string">
        The password of the user.
      </Property>
      <Property name="action" type="string">
        The action of this request, which is "remove_from_dashboard".
      </Property>
      <Property name="service" type="string">
        The name of the service you want to remove from the user's dashboard.
      </Property>
      <Property name="chain_id" type="string">
        The current chain ID.
      </Property>
    </Properties>

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="DELETE" label="/userdata">

    ```bash {{ title: 'cURL' }}
    curl --location --request DELETE 'https://api.zkp.services/userdata' \
    --header 'Content-Type: application/json' \
    --data '{
          "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
          "action": "remove_from_dashboard",
          "password": "abc123",
          "service": "id",
          "chain_id": "0xa869"
    }'
    ```

    ```js
    var settings = {
      "url": "https://api.zkp.services/userdata",
      "method": "DELETE",
      "timeout": 0,
      "headers": {
        "Content-Type": "application/json"
      },
      "data": JSON.stringify({
        "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
        "action": "remove_from_dashboard",
        "password": "abc123",
        "service": "id",
        "chain_id": "0xa869"
      }),
    };

    $.ajax(settings).done(function (response) {
      console.log(response);
    });
    ```

    ```python
    import requests
    import json

    url = "https://api.zkp.services/userdata"

    payload = json.dumps({
      "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
      "action": "remove_from_dashboard",
      "password": "abc123",
      "service": "id",
      "chain_id": "0xa869"
    })
    headers = {
      'Content-Type': 'application/json'
    }

    response = requests.request("DELETE", url, headers=headers, data=payload)

    print(response.text)
    ```

    ```powershell {{ title: 'Powershell' }}
    $headers = New-Object "System.Collections.Generic.Dictionary[[String],[String]]"
    $headers.Add("Content-Type", "application/json")

    $body = @"
    {
          `"id`": `"0xc6165a271f5cb5960c4554a8b44fcf3c75fa7f6c`",
          `"action`": `"remove_from_dashboard`",
          `"password`": `"abc123`",
          `"service`": `"id`",
          `"chain_id`": `"0xa869`"
    }
    "@

    $response = Invoke-RestMethod 'https://api.zkp.services/userdata' -Method 'DELETE' -Headers $headers -Body $body
    $response | ConvertTo-Json
    ```

    </CodeGroup>

    ```text {{ title: 'Response' }}
    "Item removed from dashboard successfully!"
    ```

  </Col>
</Row>
---

## Add a crosschain transaction {{ tag: 'POST', label: '/userdata, action: "add_crosschain_transaction"' }}

<Row>
  <Col>

    This endpoint allows you to add a new crosschain transaction.

    Authorization is required.

    ### Required attributes

    <Properties>
      <Property name="id" type="string">
        The wallet address of the user.
      </Property>
      <Property name="password" type="string">
        The password of the user.
      </Property>
      <Property name="action" type="string">
        The action of this request, which is "add_crosschain_transaction".
      </Property>
      <Property name="type" type="string">
        The type of the parameter that is to be synced to another chain.
      </Property>
      <Property name="param_key" type="string">
        The key of the parameter specified.
      </Property>
      <Property name="source_chain_id" type="string">
        The chain ID of the source chain.
      </Property>
      <Property name="target_chain_id" type="string">
        The chain ID of the target chain.
      </Property>
    </Properties>

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="POST" label="/userdata">

    ```bash {{ title: 'cURL' }}
    curl --location 'https://api.zkp.services/userdata' \
    --header 'Content-Type: application/json' \
    --data '{
        "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
        "password": "abc123",
        "target_chain_id": "0x13881",
        "source_chain_id": "0xa869",
        "action": "add_crosschain_transaction",
        "param_key": "id",
        "type": "data",
        "ccid": "1283748234"
    }'
    ```

    ```js
    var settings = {
      "url": "https://api.zkp.services/userdata",
      "method": "POST",
      "timeout": 0,
      "headers": {
        "Content-Type": "application/json"
      },
      "data": JSON.stringify({
        "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
        "password": "abc123",
        "target_chain_id": "0x13881",
        "source_chain_id": "0xa869",
        "action": "add_crosschain_transaction",
        "param_key": "id",
        "type": "data",
        "ccid": "1283748234"
      }),
    };

    $.ajax(settings).done(function (response) {
      console.log(response);
    });
    ```

    ```python
    import requests
    import json

    url = "https://api.zkp.services/userdata"

    payload = json.dumps({
      "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
      "password": "abc123",
      "target_chain_id": "0x13881",
      "source_chain_id": "0xa869",
      "action": "add_crosschain_transaction",
      "param_key": "id",
      "type": "data",
      "ccid": "1283748234"
    })
    headers = {
      'Content-Type': 'application/json'
    }

    response = requests.request("POST", url, headers=headers, data=payload)

    print(response.text)
    ```

    ```powershell {{ title: 'Powershell' }}
    $headers = New-Object "System.Collections.Generic.Dictionary[[String],[String]]"
    $headers.Add("Content-Type", "application/json")

    $body = @"
    {
        `"id`": `"0x31c1a9b5c2838bac93d0ec821ef7901b6ad5c365`",
        `"password`": `"abc123`",
        `"target_chain_id`": `"0x13881`",
        `"source_chain_id`": `"0xa869`",
        `"action`": `"add_crosschain_transaction`",
        `"param_key`": `"id`",
        `"type`": `"data`",
        `"ccid`": `"1283748234`"
    }
    "@

    $response = Invoke-RestMethod 'https://api.zkp.services/userdata' -Method 'POST' -Headers $headers -Body $body
    $response | ConvertTo-Json
    ```

    </CodeGroup>

    ```text {{ title: 'Response' }}
    "Crosschain transaction added successfully!"
    ```

  </Col>
</Row>
---

## Get crosschain transactions {{ tag: 'POST', label: '/userdata, action: "get_crosschain_transaction"' }}

<Row>
  <Col>

    This endpoint allows you to retrieve the list of crosschain transactions for a specific chain.

    Authorization is required.

    ### Required attributes

    <Properties>
      <Property name="id" type="string">
        The wallet address of the user.
      </Property>
      <Property name="password" type="string">
        The password of the user.
      </Property>
      <Property name="action" type="string">
        The action of this request, which is "get_crosschain_transaction".
      </Property>
      <Property name="chain_id" type="string">
        The current chain ID.
      </Property>
    </Properties>

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="POST" label="/userdata">

    ```bash {{ title: 'cURL' }}
    curl --location 'https://api.zkp.services/userdata' \
    --header 'Content-Type: application/json' \
    --data '{
          "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
          "action": "get_crosschain_transaction",
          "password": "pop",
          "chain_id": "0xa869"
    }'
    ```

    ```js
    var settings = {
      "url": "https://api.zkp.services/userdata",
      "method": "POST",
      "timeout": 0,
      "headers": {
        "Content-Type": "application/json"
      },
      "data": JSON.stringify({
        "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
        "action": "get_crosschain_transaction",
        "password": "pop",
        "chain_id": "0xa869"
      }),
    };

    $.ajax(settings).done(function (response) {
      console.log(response);
    });
    ```

    ```python
    import requests
    import json

    url = "https://api.zkp.services/userdata"

    payload = json.dumps({
      "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
      "action": "get_crosschain_transaction",
      "password": "pop",
      "chain_id": "0xa869"
    })
    headers = {
      'Content-Type': 'application/json'
    }

    response = requests.request("POST", url, headers=headers, data=payload)

    print(response.text)
    ```

    ```powershell {{ title: 'Powershell' }}
    $headers = New-Object "System.Collections.Generic.Dictionary[[String],[String]]"
    $headers.Add("Content-Type", "application/json")

    $body = @"
    {
          `"id`": `"0xd7b250aa0c88def87cb6b92b6d40b5b52ab3507e`",
          `"action`": `"get_crosschain_transaction`",
          `"password`": `"pop`",
          `"chain_id`": `"0xa869`"
    }
    "@

    $response = Invoke-RestMethod 'https://api.zkp.services/userdata' -Method 'POST' -Headers $headers -Body $body
    $response | ConvertTo-Json
    ```

    </CodeGroup>

    ```text {{ title: 'Response' }}
    [{"last_updated": "1701375571", "ccid": "1283748234", "parameter_key": "hair", "target_chain": "0x13881", "parameter": "data", "source_chain": "0xa869"}]
    ```

  </Col>
</Row>
---

## Get incoming {{ tag: 'POST', label: '/userdata, action: "get_incoming"' }}

<Row>
  <Col>

    This endpoint allows you to retrieve all incoming requests and responses for a specific chain.

    Authorization is required.

    ### Required attributes

    <Properties>
      <Property name="id" type="string">
        The wallet address of the user.
      </Property>
      <Property name="password" type="string">
        The password of the user.
      </Property>
      <Property name="action" type="string">
        The action of this request, which is "get_incoming".
      </Property>
      <Property name="chain_id" type="string">
        The current chain ID.
      </Property>
    </Properties>

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="POST" label="/userdata">

    ```bash {{ title: 'cURL' }}
    curl --location 'https://api.zkp.services/userdata' \
    --header 'Content-Type: application/json' \
    --data '{
        "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
        "action": "get_incoming",
        "chain_id": "0xa869",
        "password": "abc123"
    }'
    ```

    ```js
    var settings = {
      "url": "https://api.zkp.services/userdata",
      "method": "POST",
      "timeout": 0,
      "headers": {
        "Content-Type": "application/json"
      },
      "data": JSON.stringify({
        "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
        "action": "get_incoming",
        "chain_id": "0xa869",
        "password": "abc123"
      }),
    };

    $.ajax(settings).done(function (response) {
      console.log(response);
    });
    ```

    ```python
    import requests
    import json

    url = "https://api.zkp.services/userdata"

    payload = json.dumps({
      "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
      "action": "get_incoming",
      "chain_id": "0xa869",
      "password": "abc123"
    })
    headers = {
      'Content-Type': 'application/json'
    }

    response = requests.request("POST", url, headers=headers, data=payload)

    print(response.text)
    ```

    ```powershell {{ title: 'Powershell' }}
    $headers = New-Object "System.Collections.Generic.Dictionary[[String],[String]]"
    $headers.Add("Content-Type", "application/json")

    $body = @"
    {
        `"id`": `"0xce231517da5940e0880c6a2062255640916016ba`",
        `"action`": `"get_incoming`",
        `"chain_id`": `"0xa869`",
        `"password`": `"abc123`"
    }
    "@

    $response = Invoke-RestMethod 'https://api.zkp.services/userdata' -Method 'POST' -Headers $headers -Body $body
    $response | ConvertTo-Json
    ```

    </CodeGroup>

    ```text {{ title: 'Response' }}
    {"requests_received": [{"address_sender": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d", "last_updated": "1701720851", "salt": "i5YTE52s:Ev;7h%;do5LQL1d", "response_fee": "10", "updated_data": {"thisisatest": {"testarray": ["value1", "value2", "value3"]}}, "twoFARequestID": "", "attach_token": false, "address_receiver": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d", "field": "thisisatest", "chainID": "0xa869", "requestID": "2268977444251872856677282658047689095817294886379324949820237564846081429917", "limit": "600", "require2FA": false, "operation": "update", "key": "Z>SmAr{oBq<17m;1g.x}.P+!", "timestamp": "1701720838237"}], "responses_received": [{"address_sender": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d", "last_updated": "1701720904", "salt": "i5YTE52s:Ev;7h%;do5LQL1d", "response_fee": "10", "updated_data": {"thisisatest": {"testarray": ["value1", "value2", "value3"]}}, "twoFARequestID": "", "address_receiver": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d", "field": "thisisatest", "chainID": "0xa869", "limit": "600", "require2FA": false, "operation": "update", "key": "Z>SmAr{oBq<17m;1g.x}.P+!", "responseID": "2268977444251872856677282658047689095817294886379324949820237564846081429917", "timestamp": "1701720901897"}]}
    ```

  </Col>
</Row>
---

## Get outgoing {{ tag: 'POST', label: '/userdata, action: "get_outgoing"' }}

<Row>
  <Col>

    This endpoint allows you to retrieve all outgoing requests and responses for a specific chain.

    Authorization is required.

    ### Required attributes

    <Properties>
      <Property name="id" type="string">
        The wallet address of the user.
      </Property>
      <Property name="password" type="string">
        The password of the user.
      </Property>
      <Property name="action" type="string">
        The action of this request, which is "get_outgoing".
      </Property>
      <Property name="chain_id" type="string">
        The current chain ID.
      </Property>
    </Properties>

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="POST" label="/userdata">

    ```bash {{ title: 'cURL' }}
    curl --location 'https://api.zkp.services/userdata' \
    --header 'Content-Type: application/json' \
    --data '{
        "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
        "action": "get_outgoing",
        "password": "abc123",
        "chain_id": "0xa869"
    }'
    ```

    ```js
    var settings = {
      "url": "https://api.zkp.services/userdata",
      "method": "POST",
      "timeout": 0,
      "headers": {
        "Content-Type": "application/json"
      },
      "data": JSON.stringify({
        "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
        "action": "get_outgoing",
        "password": "abc123",
        "chain_id": "0xa869"
      }),
    };

    $.ajax(settings).done(function (response) {
      console.log(response);
    });
    ```

    ```python
    import requests
    import json

    url = "https://api.zkp.services/userdata"

    payload = json.dumps({
      "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
      "action": "get_outgoing",
      "password": "abc123",
      "chain_id": "0xa869"
    })
    headers = {
      'Content-Type': 'application/json'
    }

    response = requests.request("POST", url, headers=headers, data=payload)

    print(response.text)
    ```

    ```powershell {{ title: 'Powershell' }}
    $headers = New-Object "System.Collections.Generic.Dictionary[[String],[String]]"
    $headers.Add("Content-Type", "application/json")

    $body = @"
    {
        `"id`": `"0xc6165a271f5cb5960c4554a8b44fcf3c75fa7f6c`",
        `"action`": `"get_outgoing`",
        `"password`": `"abc123`",
        `"chain_id`": `"0xa869`"
    }
    "@

    $response = Invoke-RestMethod 'https://api.zkp.services/userdata' -Method 'POST' -Headers $headers -Body $body
    $response | ConvertTo-Json
    ```

    </CodeGroup>

    ```text {{ title: 'Response' }}
    {"requests_sent": [{"address_sender": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d", "last_updated": "1701720851", "salt": "i5YTE52s:Ev;7h%;do5LQL1d", "response_fee": "10", "updated_data": {"thisisatest": {"testarray": ["value1", "value2", "value3"]}}, "twoFARequestID": "", "attach_token": false, "address_receiver": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d", "field": "thisisatest", "chainID": "0xa869", "requestID": "2268977444251872856677282658047689095817294886379324949820237564846081429917", "limit": "600", "require2FA": false, "operation": "update", "key": "Z>SmAr{oBq<17m;1g.x}.P+!", "timestamp": "1701720838237"}], "responses_sent": [{"address_sender": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d", "last_updated": "1701720904", "salt": "i5YTE52s:Ev;7h%;do5LQL1d", "response_fee": "10", "updated_data": {"thisisatest": {"testarray": ["value1", "value2", "value3"]}}, "twoFARequestID": "", "address_receiver": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d", "field": "thisisatest", "chainID": "0xa869", "limit": "600", "require2FA": false, "operation": "update", "key": "Z>SmAr{oBq<17m;1g.x}.P+!", "responseID": "2268977444251872856677282658047689095817294886379324949820237564846081429917", "timestamp": "1701720901897"}]}
    ```

  </Col>
</Row>

---

## Add new chain {{ tag: 'POST', label: '/userdata, action: "add_new_chain"' }}

<Row>
  <Col>

    This endpoint allows you to onboard onto a new chain.

    Authorization is required.

    ### Required attributes

    <Properties>
      <Property name="id" type="string">
        The wallet address of the user.
      </Property>
      <Property name="password" type="string">
        The password of the user.
      </Property>
      <Property name="action" type="string">
        The action of this request, which is "add_new_chain".
      </Property>
      <Property name="old_chain_id" type="string">
        The current chain ID.
      </Property>
      <Property name="chain_id" type="string">
        The new chain ID.
      </Property>
    </Properties>

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="POST" label="/userdata">

    ```bash {{ title: 'cURL' }}
    curl --location 'https://api.zkp.services/userdata' \
    --header 'Content-Type: application/json' \
    --data '{
        "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
        "password": "maeve",
        "chain_id": "0x13881",
        "old_chain_id": "0xa869",
        "action": "add_new_chain"
    }'
    ```

    ```js
    var settings = {
      "url": "https://api.zkp.services/userdata",
      "method": "POST",
      "timeout": 0,
      "headers": {
        "Content-Type": "application/json"
      },
      "data": JSON.stringify({
        "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
        "password": "maeve",
        "chain_id": "0x13881",
        "old_chain_id": "0xa869",
        "action": "add_new_chain"
      }),
    };

    $.ajax(settings).done(function (response) {
      console.log(response);
    });
    ```

    ```python
    import requests
    import json

    url = "https://api.zkp.services/userdata"

    payload = json.dumps({
      "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
      "password": "maeve",
      "chain_id": "0x13881",
      "old_chain_id": "0xa869",
      "action": "add_new_chain"
    })
    headers = {
      'Content-Type': 'application/json'
    }

    response = requests.request("POST", url, headers=headers, data=payload)

    print(response.text)
    ```

    ```powershell {{ title: 'Powershell' }}
    $headers = New-Object "System.Collections.Generic.Dictionary[[String],[String]]"
    $headers.Add("Content-Type", "application/json")

    $body = @"
    {
        `"id`": `"0x31c1a9b5c2838bac93d0ec821ef7901b6ad5c365`",
        `"password`": `"maeve`",
        `"chain_id`": `"0x13881`",
        `"old_chain_id`": `"0xa869`",
        `"action`": `"add_new_chain`"
    }
    "@

    $response = Invoke-RestMethod 'https://api.zkp.services/userdata' -Method 'POST' -Headers $headers -Body $body
    $response | ConvertTo-Json
    ```

    </CodeGroup>

    ```text {{ title: 'Response' }}
    "New chain added successfully."
    ```

  </Col>
</Row>

---

## Get chain data {{ tag: 'POST', label: '/userdata, action: "get_chain_data"' }}

<Row>
  <Col>

    This endpoint allows you to get the full set of chain data for a user, including all chains.

    Note the response from this is very large, which is why it is omitted.

    Authorization is required.

    ### Required attributes

    <Properties>
      <Property name="id" type="string">
        The wallet address of the user.
      </Property>
      <Property name="password" type="string">
        The password of the user.
      </Property>
      <Property name="action" type="string">
        The action of this request, which is "get_chain_data".
      </Property>
      <Property name="chain_id" type="string">
        The new chain ID.
      </Property>
    </Properties>

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="POST" label="/userdata">

    ```bash {{ title: 'cURL' }}
    curl --location 'https://api.zkp.services/userdata' \
    --header 'Content-Type: application/json' \
    --data '{
          "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
          "action": "get_chain_data",
          "password": "pop",
          "chain_id": "0xa869"
    }'
    ```

    ```js
    var settings = {
      "url": "https://api.zkp.services/userdata",
      "method": "POST",
      "timeout": 0,
      "headers": {
        "Content-Type": "application/json"
      },
      "data": JSON.stringify({
        "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
        "action": "get_chain_data",
        "password": "pop",
        "chain_id": "0xa869"
      }),
    };

    $.ajax(settings).done(function (response) {
      console.log(response);
    });
    ```

    ```python
    import requests
    import json

    url = "https://api.zkp.services/userdata"

    payload = json.dumps({
      "id": "0x84713a3a001E2157d134B97C59D6bdAb351dd69d",
      "action": "get_chain_data",
      "password": "pop",
      "chain_id": "0xa869"
    })
    headers = {
      'Content-Type': 'application/json'
    }

    response = requests.request("POST", url, headers=headers, data=payload)

    print(response.text)
    ```

    ```powershell {{ title: 'Powershell' }}
    $headers = New-Object "System.Collections.Generic.Dictionary[[String],[String]]"
    $headers.Add("Content-Type", "application/json")

    $body = @"
    {
          `"id`": `"0xd7b250aa0c88def87cb6b92b6d40b5b52ab3507e`",
          `"action`": `"get_chain_data`",
          `"password`": `"pop`",
          `"chain_id`": `"0xa869`"
    }
    "@

    $response = Invoke-RestMethod 'https://api.zkp.services/userdata' -Method 'POST' -Headers $headers -Body $body
    $response | ConvertTo-Json
    ```

    </CodeGroup>

    ```text {{ title: 'Response' }}
    "{"props": {"2fa_password": "pop", "contract_password": "pop", "onboarded_chains": ["0x13881", "0xa869"], "rsa_enc_pub_key": "", "rsa_sign_pub_key": "", "public_info": "Mi chiamo Ploppini, il sono cane del tartufo"}}"
    ```

  </Col>
</Row>
